// Required Environment Variables
MORPH_ENCRYPTION_KEY          // Used for encryption/decryption of sensitive data
MORPH_CALLBACK_BASE_URL       // Base URL for OAuth callbacks
MORPH_HOOK_BASE_URL           // Base URL for OAuth callbacks

// Optional Environment Variables
MORPH_ENCRYPTION_IV_LENGTH    // Length of initialization vector for encryption (default: "16")
MORPH_SESSION_DURATION        // Duration of sessions (default: "30m")

// Dynamic Connector-Specific Variables
// These are required when using OAuth2 connectors and credentials are not provided in connector options
MORPH_${CONNECTOR_ID}_CLIENT_ID      // OAuth2 client ID for specific connector
MORPH_${CONNECTOR_ID}_CLIENT_SECRET  // OAuth2 client secret for specific connector
